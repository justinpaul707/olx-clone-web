
export interface childrenType  { children: React.ReactNode };

export interface AuthRouteProps {
    loginStatus: boolean;
}

export interface AdminSidebarType {
  isOpen: boolean;
}

export interface QueryParamsType {
  param: string;
}